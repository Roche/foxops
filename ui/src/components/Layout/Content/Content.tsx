import styled from '@emotion/styled'

export const Content = styled.div({
  paddingTop: 16,
  paddingBottom: 16,
  width: 'min(calc(90% - 52px), 100rem)',
  height: 'calc(100vh - 14rem - 60px)',
  position: 'relative'
})
